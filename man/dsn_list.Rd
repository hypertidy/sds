% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{dsn_list}
\alias{dsn_list}
\alias{dsn_info}
\title{List and filter the sds registry}
\usage{
dsn_list(pattern = NULL, theme = NULL, provider = NULL, kind = NULL)

dsn_info(name)
}
\arguments{
\item{pattern}{optional pattern matched against 'name' (grep, case
insensitive)}

\item{theme}{optional theme filter, e.g. "elevation", "bathymetry",
"imagery", "basemap", "admin"}

\item{provider}{optional provider filter, e.g. "gebco", "usgs",
"nasa_gibs", "thelist"}

\item{kind}{optional kind filter, e.g. "cog", "wmts", "parquet"}

\item{name}{a single registry name}
}
\value{
data frame, one row per source
}
\description{
Returns the registry as a data frame, optionally filtered. This is the
discovery surface: themes, providers, kinds, licenses, and liveness
status are all columns you can inspect.
}
\examples{
dsn_list(theme = "bathymetry")
dsn_list(provider = "nasa_gibs")
dsn_list("antarctic")
}
